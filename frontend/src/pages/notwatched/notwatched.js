import React from 'react';
import { connect } from 'react-redux';

const NotWatched = () => (
    <div>
        <h1>Not Watched page</h1>
        <p>Work in progress</p>
    </div>
);

export default connect()(NotWatched);
