
import React from 'react';
import { connect } from 'react-redux';

const Explore = () => (
    <div>
        <h1>Explore page</h1>
        <p>Show list</p>
    </div>
);

export default connect()(Explore);
